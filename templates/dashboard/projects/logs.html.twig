{% extends 'dashboard/base.html.twig' %}

{% block title %}{{ project.name }} - Container Logs{% endblock %}
{% block page_title %}Container Logs{% endblock %}

{% block content %}
<div class="mb-6">
    <nav class="flex text-sm text-gray-400">
        <a href="{{ path('app_projects') }}" class="hover:text-white">Projects</a>
        <span class="mx-2">/</span>
        <a href="{{ path('app_project_show', {slug: project.slug}) }}" class="hover:text-white">{{ project.name }}</a>
        <span class="mx-2">/</span>
        <span class="text-white">Logs</span>
    </nav>
</div>

<div class="mb-6">
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-2xl font-bold text-white">{{ project.name }}</h2>
            <p class="text-gray-400 mt-1">Real-time container logs</p>
        </div>
        <a href="{{ path('app_project_show', {slug: project.slug}) }}"
           class="px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-lg transition-colors">
            ← Back to Project
        </a>
    </div>
</div>

<div {{ react_component('logs/ContainerLogs', {
    projectSlug: project.slug,
    initialIsRunning: isRunning
}) }}>

</div>
{% endblock %}
