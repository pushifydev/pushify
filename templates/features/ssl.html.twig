{% extends 'base.html.twig' %}

{% block title %}Auto SSL Certificates - Pushify{% endblock %}

{% block body %}
<div class="py-20">
	<div class="max-w-6xl mx-auto px-4">
		<div class="text-center mb-16">
			<div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 text-primary text-sm font-medium border border-primary/20 mb-6">
				<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
				</svg>
				<span>SSL Certificates</span>
			</div>
			<h1 class="text-4xl md:text-6xl font-bold mb-6">
				Automated TLS/SSL Certificates<br/>
				<span class="text-primary">Zero Configuration Required</span>
			</h1>
			<p class="text-xl text-gray-400 max-w-3xl mx-auto mb-8">
				Enterprise-grade TLS 1.3 certificates automatically provisioned from Let's Encrypt with zero configuration. Automatic renewal 30 days before expiration ensures zero-downtime HTTPS for all your applications and domains.
			</p>
			<a href="/register" class="inline-flex items-center gap-2 px-8 py-4 bg-primary hover:bg-primary/90 text-white font-semibold rounded-xl transition-all">
				<span>Get Started Free</span>
				<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
				</svg>
			</a>
		</div>

		<div class="grid md:grid-cols-3 gap-8 mb-20">
			<div class="p-8 rounded-xl bg-gray-800/50 border border-gray-700">
				<div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
					<svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
					</svg>
				</div>
				<h3 class="text-xl font-bold mb-2">Instant Provisioning</h3>
				<p class="text-gray-400">TLS certificates automatically provisioned via ACME protocol within 60 seconds of domain configuration. DNS validation handled automatically with zero manual intervention.</p>
			</div>
			<div class="p-8 rounded-xl bg-gray-800/50 border border-gray-700">
				<div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
					<svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
					</svg>
				</div>
				<h3 class="text-xl font-bold mb-2">Automatic Renewal</h3>
				<p class="text-gray-400">Certificates renewed automatically 30 days before expiration with hot-reload to avoid any downtime. Monitoring alerts notify you of any renewal issues before expiration.</p>
			</div>
			<div class="p-8 rounded-xl bg-gray-800/50 border border-gray-700">
				<div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
					<svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
					</svg>
				</div>
				<h3 class="text-xl font-bold mb-2">Wildcard & Multi-Domain</h3>
				<p class="text-gray-400">Full support for wildcard certificates (*.example.com) and Subject Alternative Names (SAN) to secure multiple domains with a single certificate. TLS 1.3 with modern cipher suites.</p>
			</div>
		</div>

		<!-- Additional Features -->
		<div class="grid md:grid-cols-2 gap-8 mb-20">
			<div class="p-8 rounded-xl bg-gray-800/50 border border-gray-700">
				<h3 class="text-2xl font-bold mb-3">HTTP/2 & HTTP/3 Ready</h3>
				<p class="text-gray-400 mb-4">All SSL certificates configured for maximum performance with HTTP/2 and HTTP/3 (QUIC) protocol support. HSTS preloading and OCSP stapling enabled by default for optimal security and performance.</p>
			</div>
			<div class="p-8 rounded-xl bg-gray-800/50 border border-gray-700">
				<h3 class="text-2xl font-bold mb-3">Certificate Monitoring</h3>
				<p class="text-gray-400 mb-4">Real-time monitoring of certificate validity, expiration dates, and renewal status. Automatic alerts via email, Slack, or webhook if renewal fails or certificate issues are detected.</p>
			</div>
		</div>

		<div class="text-center p-12 rounded-2xl bg-primary">
			<h2 class="text-3xl font-bold text-white mb-4">Enterprise Security Out of the Box</h2>
			<p class="text-xl text-white/90 mb-6">Automatic TLS 1.3 certificates with zero configuration. Add your domain and deploy with HTTPS enabled in under 60 seconds.</p>
			<a href="/register" class="inline-flex items-center gap-2 px-8 py-4 bg-white text-primary font-semibold rounded-xl transition-all hover:bg-gray-100">
				<span>Get Started Free</span>
				<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
				</svg>
			</a>
		</div>
	</div>
</div>
{% endblock %}
